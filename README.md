### Installation
Clone git repository
```sh
$ git clone https://github.com/degen-vc/hardcore-frontend.git
```

Dillinger requires [Node.js](https://nodejs.org/) v10+ to run.

Install the dependencies

```sh
$ npm install
```
For run project use:
```sh
$ npm run start
```